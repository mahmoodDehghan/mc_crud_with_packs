//Should be always be implements!
class Mapper<T, G> {
  G map(T input) {
    throw UnsupportedError('should be part of implement class');
  }

  T reverse(G input) {
    throw UnsupportedError('should be part of implement class');
  }
}
